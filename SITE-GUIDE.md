# 网站使用指南

## 🚀 快速创建新文章

### Windows PowerShell
```powershell
# 创建笔记
.\new-post.ps1 -type posts -name "文章标题"

# 创建笔记到子目录
.\new-post.ps1 -type posts -name "密码学第二讲" -subfolder "Courses/crypto"

# 创建旅行日记
.\new-post.ps1 -type travel -name "杭州西湖游记"

# 创建 Her 日记
.\new-post.ps1 -type her -name "今日份甜蜜"
```

### WSL / Linux
```bash
chmod +x new-post.sh  # 首次使用需要添加执行权限

# 创建笔记
./new-post.sh posts "文章标题"

# 创建笔记到子目录
./new-post.sh posts "密码学第二讲" "Courses/crypto"

# 创建旅行日记
./new-post.sh travel "杭州西湖游记"
```

### 文章类型
| 类型 | 说明 |
|------|------|
| `posts` | 笔记/技术文章 |
| `travel` | 旅行&户外 |
| `her` | Her 日记 |
| `contemplate` | 沉思 |
| `projects` | 项目 |
| `guide` | 导航 |

---

## ✨ 网站功能

### 1. 首页背景图
- 背景图位置: `static/images/bg-home.jpg`
- 只在首页显示，其他页面不显示
- 支持深色/浅色模式自动适配

### 2. 阅读进度条
- 文章页面顶部会显示紫色渐变进度条
- 随滚动自动更新

### 3. 全站搜索
- 点击导航栏的 🔍 图标
- 支持搜索所有文章标题和内容

### 4. 评论功能 (Giscus)
- 基于 GitHub Discussions
- 文章底部自动显示评论区
- 需要 GitHub 账号登录评论

### 5. 数学公式 (KaTeX)
在文章 front matter 中添加 `math: true`：
```yaml
---
title: "我的文章"
math: true
---
```

公式语法：
- 行内公式: `$E = mc^2$`
- 块级公式: 
```
$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$
```

### 6. 文章功能
- 📖 阅读时间估算
- 📊 字数统计
- 📑 目录导航 (TOC)
- 📋 代码复制按钮
- 🔗 面包屑导航
- ⬆️ 返回顶部按钮

---

## 📝 Markdown 写作技巧

### 换行
- 单个换行会自动渲染（已启用 hardWraps）
- 或者在行末加两个空格
- 或者使用 `<br>`

### 图片
```markdown
![图片描述](/images/your-image.jpg)
```
图片放在 `static/images/` 目录下

### 引用
```markdown
> 这是一段引用
```

### 代码块
````markdown
```python
print("Hello World")
```
````

---

## 🛠️ 常用命令

```bash
# 本地预览
hugo server

# 构建网站
hugo

# 创建新文章（Hugo 原生方式）
hugo new posts/my-post.md
```

---

## 📁 目录结构

```
├── content/           # 文章内容
│   ├── posts/        # 笔记
│   ├── travel/       # 旅行
│   ├── her/          # Her
│   ├── contemplate/  # 沉思
│   ├── projects/     # 项目
│   └── guide/        # 导航
├── static/images/    # 图片资源
├── assets/css/       # 自定义样式
└── layouts/          # 自定义模板
```
